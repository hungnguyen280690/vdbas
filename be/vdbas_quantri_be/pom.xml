<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.5</version>
        <relativePath/>
    </parent>

    <groupId>com.fis.vdbas.qtdc</groupId>
    <artifactId>vdbas-qttt-be</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>vdbas-qttt-be</name>
    <description>Nền tảng số QTTT - Backend (Spring Boot Maven multi-module)</description>

    <properties>
        <java.version>21</java.version>
        <spring-boot.version>4.0.6</spring-boot.version>
        <spring-security.version>7.0.5</spring-security.version>
        <spring-framework.version>7.0.7</spring-framework.version>
        <spring-cloud.version>2025.1.0</spring-cloud.version>
        <keycloak.version>26.0.9</keycloak.version>
        <ojdbc.version>21.11.0.0</ojdbc.version>
        <tomcat.version>11.0.21</tomcat.version>
        <commons-io.version>2.21.0</commons-io.version>
    </properties>

    <modules>
        <module>common</module>
        <module>domain</module>
        <module>application</module>
        <module>api</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <!-- <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${project.parent.version}</version>              
                <type>pom</type>
                <scope>import</scope>
            </dependency> -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Thêm khai báo quản lý cho module common của bạn -->
            <dependency>
                <groupId>com.fis.vdbas.qtdc</groupId>
                <artifactId>common</artifactId>
                <version>${project.version}</version> <!-- Nó sẽ lấy 0.0.1-SNAPSHOT -->
            </dependency>
            
            <!-- Tương tự, nếu domain được module khác dùng, hãy thêm vào đây -->
            <dependency>
                <groupId>com.fis.vdbas.qtdc</groupId>
                <artifactId>domain</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Thêm khai báo quản lý cho module common của bạn -->
            <dependency>
                <groupId>com.fis.vdbas.qtdc</groupId>
                <artifactId>api</artifactId>
                <version>${project.version}</version> <!-- Nó sẽ lấy 0.0.1-SNAPSHOT -->
            </dependency>
            
            <!-- Tương tự, nếu domain được module khác dùng, hãy thêm vào đây -->
            <dependency>
                <groupId>com.fis.vdbas.qtdc</groupId>
                <artifactId>application</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons-io.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fis.vdbas</groupId>
                <artifactId>vdbas-common</artifactId>
                <version>1.0.0-SNAPSHOT</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <repositories>
        <repository>
            <id>nexus-snapshots</id>
            <url>https://ac-nexus.apps.ocp.vst.gov.vn/repository/maven-snapshots/</url>
            <snapshots><enabled>true</enabled></snapshots>
            <releases><enabled>false</enabled></releases>
        </repository>
    </repositories>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>





